/*
 * Example config for local testing only.
 * --------------------------------------
 * This file is for documentation purposes and should NOT be used as runtime code.
 * For production, use GitHub repository Secrets and let the deploy workflow generate config.js.
 *
 * If running locally, create a config.js with:
 *   window.SUPABASE_URL = 'https://your-project.supabase.co';
 *   window.SUPABASE_ANON_KEY = 'your-anon-key';
 * DO NOT commit real keys to the repo.
 */

// No runtime code here; this file is documentation only.
