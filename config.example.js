/*
  Example config for local testing only.
  For safer deploy to GitHub Pages, don't commit a real `config.js` to the repo.
  Instead, add `SUPABASE_URL` and `SUPABASE_ANON_KEY` as GitHub repository Secrets
  and let the deploy workflow generate `config.js` at build-time.

  If you want to run locally without GH Actions, create a `config.js` with the
  following contents (DO NOT commit it):

  window.SUPABASE_URL = 'https://your-project.supabase.co';
  window.SUPABASE_ANON_KEY = 'your-anon-key';

*/

// No runtime code here; this file is documentation only.
